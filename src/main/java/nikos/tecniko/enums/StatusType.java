package nikos.tecniko.enums;

/***
 * Enum to define the Status of the Repair(PENDING, DECLINED, ACCEPTED, IN_PROGRESS, COMPLETE)
 */
public enum StatusType {
    PENDING, DECLINED, ACCEPTED, IN_PROGRESS, COMPLETE
}
